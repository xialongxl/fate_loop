# 《命运轮回：FATE LOOP》交接文档

> 上一版：阶段 9 代码写完但一行没接线的时候。
> 本版基于当前工作区逐文件核查后写成：`eslint` 干净、`vitest run` **466 测试全绿（14 文件）**、`vite build` 成功，并且**已有 git 仓库**（14 个提交，可回滚）。
> 重点是**尚未完成的工作**与**下一步思路**。已完成部分只做索引，够定位代码即可。
> 只有 10 分钟的话：读「三、当前真实状态」与「七、未完成工作」。**P0 与 P1 的
> 功能项都已清零，一局可以打通（第 50 层通关 + 通关后可选无尽）**；剩下的都是
> 平衡与清理类：解锁天花板与经验曲线错配、`orbSlots` 去留、第八节列的四个新缺口。

---

## 一、这个项目是什么

纯文字、模组驱动、**确定性**的 Roguelike 自动战斗工具。

玩家不操作战斗。玩家做的事是：**编排技能序列**（GCD 循环队列 + oGCD 优先级槽），然后让角色自动打，观察结果，回去改序列。战斗过程是纯函数：给定 `(seed, 序列配置, 内容模组)`，逐帧结果必然一致，1x / 4x / MAX 三种倍速下的最终状态**逐字节相同**。

技术栈：原生 JS（ESM，无框架）+ Vite + Vitest + Howler（音频，可选）。无 TypeScript，用 JSDoc 标注类型。UI 是手写 DOM + 模板字符串，地图用 SVG。

**为什么强调确定性**：这是整个项目的立项卖点。种子是复现整局的完整凭据。所有涉及随机、时间、浮点的改动都必须先想清楚是否破坏这一点 —— 见「五、不可违反的内核不变量」，其中一条现在有测试守着了（`app-wiring.test.js` 的「渲染再多也不消费随机流」指纹对拍）。

---

## 二、代码地图

```
F:/down/fate_loop
├── index.html               入口指向 /src/boot.js
├── docs/                    设计与计划文档（文档债已清，见第十节）
│   └── archive/             两份旧项目（Boss Rush 弹幕）文档，已隔离并加声明
├── src/
│   ├── boot.js              ★ 浏览器入口：只做装配 + 失败时把错误摊上屏
│   ├── main.js              ★ 应用装配工厂 createApp()（792 行）。导出而非自动启动，
│   │                          为的是能在 jsdom 冒烟测试里整局驱动
│   ├── core/                纯逻辑，禁止碰 DOM
│   │   ├── constants.js     所有枚举与调参常量的单一来源
│   │   ├── prng.js          Mulberry32 + deriveSeed 三流分派 + normalizeSeed/fnv1a
│   │   ├── store.js         不可变状态容器（update(draft) + 订阅 + replace）
│   │   ├── initialState.js  createPlayer（含 permanentBonus）/ createInitialState
│   │   ├── entity.js        createEntity 与 GCD/oGCD 就绪判定
│   │   ├── derived.js       ★ recalcPlayer / derivePlayerStats / addPermanentBonus
│   │   ├── progression.js   等级/经验曲线、baseStatsAtLevel、buildUnlockTable
│   │   ├── equipment.js     装备栏、词条、强化、分解、describeGear
│   │   ├── loot.js          掉落与商店装备生成
│   │   ├── game.js          ★ GameFlow：探索流程编排（移动/下层/休息/商店/事件/装备）
│   │   ├── battle/          engine.js（主循环+倍速+日志）/ scheduler.js / resolution.js
│   │   ├── contracts/       契约注册表（damage/heal/buff/prng/audio 等）
│   │   └── mods/            loader.js（拓扑排序加载）+ normalize/graph/manifest
│   ├── mods/official/       官方内容模组（技能/怪物/遭遇/地图/商品/事件）
│   ├── persistence/         saveService.js（含 clearAll）+ schema.js + adapters/
│   └── ui/
│       ├── styles.css       ★ 2324 行，阶段 9 各屏样式已补全
│       ├── shell.js         外壳 + 局内导航条
│       ├── router.js        ScreenRouter（go/back/renderCurrent）
│       ├── dialog.js        ★ 模态框：ESC、焦点陷阱、openSummary
│       ├── format.js        escapeHtml / formatNumber / formatSigned / createToast / createConfirm
│       ├── screens/         ★ 10 个屏幕：mainMenu map battle character equipment
│       │                      sequence saves settings **codex** **history**
│       ├── map/             renderer.js / interaction.js / viewState.js（SVG 地图）
│       └── audio/           howlerAudio.js（无音频文件时静默降级）/ nullAudio / soundMap / throttle
└── tests/                   14 个文件，466 测试
    ├── helpers.js           注入模组的装配助手 + battleFingerprint
    ├── unit/                gameflow(88) equipment(44) contracts(42) mod-loading(54)
    │                        determinism-core(27) progression(27) map-generation(27)
    │                        persistence(35) scheduler(18) audio(10)
    └── integration/         cross-speed(10) app-wiring(58，jsdom 冒烟/暂停/通关/错误边界)
                             screens(20，屏幕细测+无障碍) balance(5，开局可用性)
```

规模：`src` 80 个 JS 文件约 11500 行，`tests` 14 个文件 466 测试约 6600 行。

**已删除**（阶段 6 遗留）：`ui/layout.js`、`ui/panel/`（含旧 `summary.js`）、`utils/ringBuffer.js`。

---

## 三、当前真实状态

### 完成度

阶段 0 到阶段 9 **全部完成并接线**。`npm run dev` 打开页面看到的是多屏外壳：主菜单 → 新的轮回（选种子）→ 地图/技能轴/装备/角色/图鉴/战绩六个局内屏 + 战斗屏 + 存档屏 + 设置屏。上一版的 P0-新（1 级合法序列胜率 38%）也已经修好，现在实测 76%。

一局**可以打通**了：使用第 50 层的出口即通关（`outcome:'victory'` 写进历史、清自动槽、
弹结算面板），面板给两条出路 —— 「继续挑战无尽」回到本层出口并置 `victoryAchieved`，
「结束这局」回主菜单。无尽段没有第二次结算，死在无尽里的记录带「通关后 · 无尽」标记。

还剩的都是平衡与清理，不是功能空缺：

1. **解锁天花板错配**：`SKILL_UNLOCK_MAX_LEVEL=120` 与"50 层≈48 级"不匹配，
   解锁表后半段是死内容（本轮按决策**没动**曲线，数据见附录）
2. **仓库不带音频资源**：这是有意的（`AUDIO_ASSETS` 为空 → 不构造 Howl、不发请求、
   不产生 404）。要出声就往 `public/audio/` 放文件并在 `soundMap.js` 登记后缀
3. **遭遇模板最深 999 层**：更深的无尽段回退到最深一档（不崩，但难度不再爬升）

### 内容量（已实装）

| 类别 | 数量 |
|---|---|
| 技能 | 90（60 GCD + 30 oGCD）|
| Buff | 14 |
| 怪物 | 300 |
| 遭遇模板 | 100（普通 60 / 精英 40）|
| 商店商品 | 9 |
| 事件 | 6 |
| 地图生成器 | 1（网格） |

### 验证状态

```
npx eslint .          干净
npx vitest run        380 passed (10 files)
npx vite build        成功
npm run verify        上面三步串联
```

测试覆盖已经不只是 core：`GameFlow` 探索流程 73 例、UI 接线 41 例（jsdom）、派生/存档/内容守卫若干。但**战斗引擎内部仍是最厚的部分，`scheduler.stepEntity` 依然只有间接覆盖**（见第八节）。

---

## 四、五分钟上手

```bash
cd /f/down/fate_loop
npm install
npm run dev        # 本地开发服务器
npm run lint       # eslint
npm test           # vitest run
npm run test:watch
npm run build      # vite build
npm run verify     # lint + test + build 串联
```

**已有 git 仓库**，`.gitignore`（node_modules/dist/coverage）与 `.gitattributes`（统一 LF）都在。当前提交序列：

```
bcb8c3c test(ui): 接线测试补路由器契约与 5 层长跑冒烟（41 例）
c5fc667 feat(ui): 阶段 9 样式补全（P0-2）并删除阶段 6 遗留（步骤 6）
ed33c78 feat(ui): 接线阶段 9 多屏外壳，main.js 从 createApp 工厂（P0-1）
27c1c46 test: 补 GameFlow 探索流程测试网（73 例）
25561ef fix(core): 永久属性改走 permanentBonus（P0-3）
ea6256e chore: 添加 .gitattributes 统一 LF
091cdf8 chore: 接手前基线快照
```

改坏了可以 `git diff 091cdf8` 对比原始状态。


---

## 五、不可违反的内核不变量

改代码前必须理解这六条。违反其中任何一条，确定性就崩了，而且**测试不一定立刻报错**（会在别的种子、别的倍速下才暴露）。

### 1. 三流随机分派

`prng.js` 用 `deriveSeed(masterSeed, streamName)` 把主种子派生成互不干扰的独立流：

- **map 流** —— 地图生成
- **battle 流** —— 战斗内随机（暴击、随机目标、随机 Buff）
- **player 流** —— 玩家开局 seedBonus

**规则**：绝不跨流借随机数，绝不在流内插入或删除一次 `next()` 调用而不评估影响。战斗流的调用次数必须与倍速无关。UI 想要随机数（比如打乱展示顺序），用 `Math.random()`，不要碰 PRNG 流。

UI 层新增了两条守门测试：开局走种子对话框不消费任何流；战斗屏渲染 50 次后的终态指纹与安静跑完全一致。

### 2. 绝对到期时间戳

冷却与 Buff 存的是**绝对到期虚拟时刻**（`readyAt`、`expiresAt`），不是剩余时间。

**为什么**：剩余时间需要每帧递减，递减次数依赖帧数，帧数依赖倍速 → 倍速会改变结果。绝对时间戳只需比较 `virtualTime >= readyAt`，与推进步长无关。

**规则**：任何新增的计时字段都必须用绝对时间戳，不许写「剩余 N 毫秒」。

### 3. 时间量必须是 STEP_MS 整数倍

所有技能施法时间、冷却、Buff 持续都必须对齐到 `STEP_MS`（16ms）。模组加载时的 `normalize` 会强制这一点，`tests/unit/mod-loading.test.js` 有对应断言。

**规则**：新增内容时别写 `cooldown: 2500`，先确认能被 `STEP_MS` 整除。

### 4. 属性单一数据源

玩家的 `maxHp / attack / defense / critChance` **从不手写**，一律由
`(exp, equipment, seedBonus, permanentBonus)` 经 `recalcPlayer` 重算。存档只存
`exp`、装备、当前 `hp`、`permanentBonus`。调用时机：升级、穿脱装备、强化、
读档、商店消费、事件结算。

上一版这条被商店与事件违反了（花 40 碎片买的「磐石之核」一次重算后归零）。
**现已修复并有三道防线**：

1. 内容只允许走 `addPermanentBonus(player, {...})`（`derived.js`）
2. `GameFlow.purchase` / `resolveEvent` 在 `apply` 后统一 `recalcPlayer`
3. `tests/unit/mod-loading.test.js` 有个守卫测试，扫全部官方商品/事件的
   `apply` 源码，出现 `state.player.maxHp = …` 之类赋值就直接红灯

**要加新的永久成长属性时**：只改 `PERMANENT_BONUS_FIELDS` 一处。

### 5. 音频永不影响逻辑

`AUDIO_PLAY` 契约返回 `undefined`、不消费随机数、sink 抛错不向上传播、sink 为 `null` 时静默丢弃。`tests/unit/contracts.test.js` 有四条测试锁定这个行为。

**规则**：不要让音频调用出现在会影响状态的表达式里。

### 6. UI 不得触碰活状态（本轮新增）

`store.unsafeGetState()` 返回的是**可写的内部状态对象**，只给 core 与测试用。
装配层的唯一取数入口是订阅回调缓存的快照：`main.js` 里 `store.subscribe(snap => { latest = snap; renderAll(); })`，所有屏幕通过 `getSnapshot()` / `getState()` 拿它。

**为什么**：一是防误写（快照深冻结），二是省掉重复克隆 —— 每帧只克隆一次，
屏幕再多也不会翻倍。历史上有个真实坑：`descend()` 用 `unsafeGetState()` 拿到的
活对象读 `floorNumber`，`enterFloor` 就地改完之后才 return，于是「下第 1 层」
返回了 `floorNumber: 3`。

**规则**：屏幕不许自己调 `store.update`，一律走 `GameFlow` 方法；
装配层要改状态（例如测试里强制战败清空序列）必须显式注释说明。

### 平局打破规则

oGCD 候选排序是 `(槽位优先级降序 → 技能自带优先级降序 → skillId 字典序)`。字典序那一档是**必须的**：没有它，同优先级技能的执行顺序会依赖 `Map` 迭代顺序。

oGCD 抢占作用域是「每实体每步至多一个」，**不是全局一个**。

---

## 六、本轮完成了什么（便于对照上一版交接文档）

| 上一版的项 | 现状 |
|---|---|
| P0-1 接线阶段 9 多屏 UI | **完成**。`main.js` 重写为 `createApp()` 工厂，入口拆到 `boot.js`；补出缺失的 `codex.js` / `history.js`；屏幕接口约定统一为 `{element, render?, onEnter?, onExit?}`（router 已用可选链，无需额外约定层） |
| P0-2 补 styles.css（缺 152 类） | **完成**。745 → 2324 行，交叉比对脚本复跑为 0 缺失；新增两档响应式与 `prefers-reduced-motion` |
| P0-3 商店永久属性被 recalc 抹掉 | **完成**。走 `permanentBonus`，并进存档（v2 可选字段，不升版本）；角色屏新增「永久加成」列 |
| P1-1 settings 无消费者 | **完成**。muted/volume 接音频、defaultSpeed 与 autoStartBattle 接战斗流程、`logLimit` 只做展示裁剪（有指纹对拍守着）、`showDamageNumbers` 无人消费已删、`SaveService.clearAll` 接上设置屏 |
| P1-2 解锁未在运行时生效 | **完成**。`GameFlow` 自己持有 `unlockTable`（屏幕共用同一张），新增 `sanitizeSequence()`，在 `startBattle()` 与 `restoreRun()` 两处剔除未解锁项（剔除而非报错）；序列屏改动后经 `safe()` 再洗一次并 toast。scheduler 每步零额外开销 |
| P1-3 mapScreen 的 battle 动作断链 | **完成**，并顺带把装备货架接进商店对话框（`getShopGear`/`purchaseGear` 此前实现却无入口） |
| P0-新 开局胜率坍塌 | **完成**（方案 A）。解锁表按类型分名额：`STARTER_GCD_COUNT=6` + `STARTER_OGCD_COUNT=4` 落在 1 级；默认序列补回两个 oGCD 槽（`secondWind` 自保 / `suddenStrike` 爆发）。胜率 38% → **76%**，并由 `tests/integration/balance.test.js` 固化为确定性断言（下限 70%） |
| 新发现洞 1：purchase 可买未上架商品 | **完成**。`notOnShelf` 校验 + 用例（读档后货架未重建时不误拦） |
| 测试缺口 1：`scheduler.stepEntity` 无直接测试 | **完成**（18 例：抢占选择、平局打破、目标解析、循环队列、悬空 ID 兜底） |
| 测试缺口 2/4：屏幕细测、audio throttle | **完成**（`screens.test.js` 20 例、`audio.test.js` 10 例） |
| P2 README / 音频清单 | **完成**（根目录 `README.md`；`AUDIO_ASSETS` 空表 ⇒ 不构造 Howl，不再有一堆被静吞的 404） |
| P1-4 对话框三个问题 | **完成**：参数名对齐（Promise 不再永久挂起）、ESC + Tab 焦点陷阱 + 焦点归位、`openSummary` 迁入新 dialog，旧 `panel/summary.js` 已删 |
| P1-5 暂停不存在 | **完成**。`engine.pause()/resume()/togglePause()/isPaused()` 真正读写 `GAME_STATUS.PAUSED`；P 键绑定；切后台自动暂停；暂停态冻住虚拟时间、速度按钮仍可用、手动存档入口关闭 |
| P1-7 错误边界 | **完成**。`reportError()` 停循环 + 写 `state.error`（只存 code/消息，不存 stack）+ 弹「出了点问题」面板（返回主菜单 / 关掉继续）；屏幕回调经 `safe()` 包裹；`window` 的 error 与 unhandledrejection 也接上 |
| P2 死代码 / dist | **完成**：删 `ringBuffer.js`、`layout.js`、`panel/`；`.gitignore` 覆盖 `dist/` |
| 第九节 文档债 | **完成**：两份旧项目文档移入 `docs/archive/` 并加声明；`技术实施细化方案.md` 的 vitest 版本改为 3.2.7 |
| P1-6 胜利结局 | **完成**（按决策：终点第 50 层 + 通关后可选无尽，曲线不动）。见下面「通关是怎么实现的」 |

---

## 七、未完成工作

### ~~P0-新：开局胜率坍塌~~ 已修（保留结论，避免你重新推导一遍）

上一轮把默认序列改成「1 级合法」之后，实测第 1~3 层胜率从 90% 掉到 38%。根因不在
GCD 选哪几个（把旧 GCD 去掉 oGCD 也是 40%），而在解锁表：`buildUnlockTable` 原本是
「全局前 N 个技能落 1 级」，而排序里 GCD 恒排在 oGCD 之前 ⇒ 30 个 oGCD 全部落在
79~120 级，唯一自保技 `ogcd.emergencyHeal` 要 94 级，而按现曲线 50 层才 48 级。
也就是说 oGCD 这套「插入技」作为本作卖点，玩家一辈子见不到。

**已采用方案 A 修掉**：名额按类型分（前 6 个 GCD + 前 4 个 oGCD 在 1 级），默认序列
补回 `ogcd.secondWind`（短冷却自疗，优先级 95）与 `ogcd.suddenStrike`（30）。
胜率 38% → **76%**。回归门：`tests/integration/balance.test.js`（本作全链路确定，
所以胜率可以直接固化成断言，下限 70%）。

**仍然遗留**：`SKILL_UNLOCK_MAX_LEVEL = 120` 与可达等级错配 —— 41 级（第 30 层）
只能见到约 1/4 技能，解锁表后半段是死内容。这条与 P1-6 胜利条件绑在一起决策，见附录。

### P1 档 —— 只剩一条清理项

- **`RARITIES[].orbSlots` 无消费者**（宝珠系统不存在）。图鉴屏会如实标注
  「宝珠槽 N（系统未实装）」，但字段仍留在常量里。要么实装要么删。

已清：P1-1 设置消费、P1-2 运行时解锁校验、P1-3 battle 动作与装备货架、
P1-4 对话框三修、P1-5 真暂停、P1-6 胜利结局、P1-7 错误边界。

### 通关是怎么实现的（P1-6，接手前先读这段）

**决策**：终点 = 第 50 层（`VICTORY_FLOOR`），通关后可选无尽，经验曲线与
`SKILL_UNLOCK_MAX_LEVEL` 本轮**不动**。

- **触发点**：`GameFlow.descend()` 里判 `floorNumber >= VICTORY_FLOOR && !victoryAchieved`
  → 走 `#settleVictory()` 而不是生成第 51 层。写 `status=FINISHED` +
  `winner=PLAYER` + `battleEndReason='victory'` + `floorsCleared++`，
  `appendHistory({outcome:'victory'})`，并**清掉自动槽**（留着会让「继续游戏」把玩家
  放回一个已通关、再点出口什么都不发生的节点）。
- **继续无尽**：`GameFlow.continueEndless()` 把状态放回 EXPLORING、清 winner，
  `victoryAchieved` 保持 true ⇒ 之后每次下层都是普通下层，永不再结算。
  无尽段死亡另记一条 `outcome:'death'` + `victoryAchieved:true`，战绩屏标
  「通关后 · 无尽」，不冒充第二次通关。
- **状态与存档**：新增 `state.victoryAchieved`，随 `serializeRun` 入档，
  `summarizeSave` 带出来（存档屏打「已通关」标签）。**老存档缺字段按 false 处理，
  没升 schema 版本** —— 理由同 `permanentBonus`：纯增量、默认值语义正确。
  不带这个标记的话，从第 51 层读档再下层会补触发一次通关。
- **一个必须记住的守卫**：通关结算也把状态写成 `FINISHED + winner=PLAYER`，
  所以 `finishBattle()` 现在会先检查 `activeBattle === null` 并返回
  `{settled:false}`。少了这道守卫，rAF 循环会在通关后再调一次结算并踩空。
- **UI**：终点层的出口按钮改成「踏入轮回尽头（第 50 层通关）」；
  `dialog.openSummary` 现在支持 `secondaryLabel/onSecondary`，并且**结算面板的按钮
  不再走 `onClose`** —— 两个出路时 `onClose` 会把玩家没选的那条也执行一遍。
- **深度的尽头**：官方遭遇模板 `maxFloor: 999`。`pickEncounter` 现在在本层无货时
  回退到「minFloor 不超过本层的最深一档」（仍是纯函数，不消费随机数），
  所以第 1200 层也不会把一局好局打死；代价是 999 层以上难度不再爬升。

#### 本轮实现里值得知道的三个取舍

1. **解锁校验放在 `GameFlow` 而不是 `scheduler`。** `GameFlow` 构造时就
   `buildUnlockTable(pool.skills)` 并对外暴露 `flow.unlockTable`（屏幕共用同一张，
   `main.js` 不再自己建）。清洗点在 `startBattle()` 与 `restoreRun()` 两处，
   **剔除而不是报错** —— 报错会让玩家卡在"打不了仗"。等级取
   `levelFromTotalExp(exp)` 而非 `player.level`：读档时后者还是旧值，要等
   `recalcPlayer` 才更新。`scheduler` 里没有查表，每步零额外开销。
2. **暂停 = 停止推进虚拟时间**，且 `GAME_STATUS.PAUSED` 现在真的会出现在状态里。
   连带两个必要修正：战斗屏速度按钮在暂停态必须仍可点（否则按下 ⏸ 就再也开不回来）；
   暂停中不给手动存档入口 —— 因为 `GameFlow` 的结算/读档路径会把状态写成 `EXPLORING`，
   暂停中的战斗存进去就恢复不出来（这是个真陷阱，已写成测试）。
   `restoreRun` 里的序列清洗特意放在 `log = []` 之后，否则通知会被自己抹掉。
3. **`logLimit` 只裁剪展示数组**（`battleScreen` / `mapScreen` 收一个 `getLogLimit`），
   `state.log` 容量仍固定 `LOG_CAPACITY`。有一条测试直接拿这个当确定性证明：
   同一场战斗在 100 / 50 两档设置下 **battleFingerprint 必须逐项相等**。
   重绘缓存的键也从"长度"改成 `长度*1000+limit`，否则改设置后条数没变就永远不重绘。

### 接线时看到的一个语义细节（非缺陷，别踩）

- **地图上的死路节点是单向邻接**（`generator.js` 里写明是有意为之：死路列出邻居
   以便渲染连线，邻居不反向列出，于是 `areAdjacent` 天然拒绝进入）。副作用是
   `GameFlow.moveTo` 里的 `deadEnd` 分支永远走不到。已用测试固化该语义，
   后来人不要再以为「死路是双向边」。

### P2 档（原样保留，都还没做）

- **`deepFreeze` 对 Map/Set 只冻结容器**，内部条目仍可增删。`shopStates`（`Map`）、
  `visitedNodeIds` / `clearedNodeIds`（`Set`）都在状态里，快照的不可变性有洞。
  要么补递归冻结，要么把注释改成如实描述。
- ~~`public/audio/` 不存在~~ **已收口**：`soundMap.js` 的 `AUDIO_ASSETS` 空表 =
  明确「本仓库不带音频」，`HowlerAudio.preload()` 对空 `src` 直接跳过，不再产生 404。
  加音频 = 放文件 + 在 `AUDIO_ASSETS` 登记后缀。`tests/unit/audio.test.js` 守着这个形状。
- **`src/mods/dev/` 目录不存在**，但 loader 已支持它（最低优先级加载）。补一个
  示例模组价值很高 —— 它同时就是模组作者的文档。
- ~~无 README~~ **已补**：根目录 `README.md`（定位、快速开始、结构、六条不变量、
  测试职责表、模组入门、已知限制）。
- **`store.js` 没有 `reset()`**。装配层现在用 `store.replace(createInitialState(...))`
  绕过，`main.js` 里已封装成 `freshState()`，但「换局」仍要记得同时复位
  speed/视图/路由栈 —— 这类隐性配套是 bug 温床，值得在 Store 上收口。

---

## 八、测试现状与缺口

现状：`tests` 14 个文件 434 测试。core 覆盖厚，UI 覆盖从 0 变成「冒烟 + 细测 + 长跑」。

| 已有 | 文件 |
|---|---|
| 跨速度对拍（1x/4x/MAX 终态逐项相等） | `integration/cross-speed.test.js` |
| 探索流程 74 例（移动/下层/休息/商店/事件/装备/读档） | `unit/gameflow.test.js` |
| oGCD 抢占、平局打破、目标解析、GCD 循环队列 | `unit/scheduler.test.js` |
| 屏幕细测（序列编辑/装备操作/排序/无障碍与焦点陷阱） | `integration/screens.test.js` |
| 开局可用性：1 级两类手段、默认序列胜率下限 | `integration/balance.test.js` |
| 音频清单形状、节流器（假时钟）、nullAudio | `unit/audio.test.js` |
| UI 接线 41 例（jsdom：外壳/路由/十屏/对话框/存档设置/5 层长跑） | `integration/app-wiring.test.js` |
| 派生属性与永久加成、强化、掉落、内容守卫 | `unit/equipment.test.js`、`unit/mod-loading.test.js` |
| 存档三适配器、槽位、序列化纯函数、permanentBonus 往返 | `unit/persistence.test.js` |

缺口按价值排序：

1. **`scheduler.stepEntity` 无直接测试。** oGCD 抢占与平局打破是确定性的核心，
   目前只被战斗整体测试间接覆盖。
2. **屏幕内部行为只测到「渲染出内容 + 回调打到 GameFlow」**，没测结构细节
   （排序、筛选、拖拽改序列）。jsdom 下这些都可测，`app-wiring.test.js` 已有现成脚手架
   （`mount / must / click / tick / standOn / shopSelling`）。
3. **audio throttle 无测试**（去重窗口与 8 路上限）。
4. **无障碍没有断言**：ESC 关弹窗已实现并有一例测试，但焦点陷阱、`aria-*` 完整性
   没测。

### UI 的视觉自检怎么做到不靠眼睛

CSS 是闭眼写的（jsdom 不渲染），而接手人用的模型不一定支持看图。可行的办法是
**用无头浏览器量 DOM**，把"画歪了"变成机器可读的结论：

1. 临时放一个 `preview.html` + `preview.js` 在项目根（**不要提交**，否则 eslint 会因为
   顶层 `document` 报 27 个错），`preview.js` 里 `import { createApp } from './src/main.js'`，
   按 `?screen=xxx` 把应用驱动到目标界面（开局、打两场胜仗、捡装备、升级、开对话框）。
2. 无头 Edge 出图或出 DOM：
   ```bash
   EDGE="/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe"   # 本机实测可用
   "$EDGE" --headless --disable-gpu --no-sandbox --disable-software-rasterizer      --window-size=1440,900 --virtual-time-budget=30000      --dump-dom "http://localhost:5602/preview.html?screen=map"   # 或 --screenshot=x.png
   ```
   `--virtual-time-budget` 要给够（30000）：codex 有 300 条怪物、saves/history 要等 IDB。
   旧版 `--headless` 可用，`--headless=new` 在这台机器上会因 GPU 进程起不来而 FATAL。
3. 脚本末尾跑一遍几何与样式体检，把结论写进 `<pre id="audit-result">`：横向/纵向溢出、
   零高度可见元素、文本被裁且没写 ellipsis、面板中心被谁遮挡（模态开着时跳过）、
   **文字对比度 < 4.5:1**、当前屏是否几乎是空的，外加一行几何数据（屏高占比、
   地图节点数、可达节点、实体卡数、列表行数）。

这一轮靠它抓出 3 个真实缺陷（都不是 jsdom 测试能发现的）：

| 缺陷 | 修法 |
|---|---|
| `.btn-primary` / `.filter-btn.is-active` / `.speed-btn.is-active` 对比度只有 **3.4:1**，不过 WCAG AA | 新增 `--accent-press:#175abf`，主按钮改白字（6.5:1），悬停回 `--accent-dim`（4.6:1） |
| `settings.setStorageInfo()` 有定义无调用方 ⇒ 设置屏"数据去向"那行永远是空段落 | `main.js` 在 init 后注入，并加回归测试 |
| 局外界面残留一排 `hidden` 的空 chip，`.header-stats` 塌成零高度占位 | 非局内屏直接 `display:none` 整条 |

现成脚手架（含上面那段体检脚本）另存了一份在仓库外：
`F:/down/preview_shots/preview.{js,html}` 与当轮的 16 张截图（1440 与 820 两档）。

一条经验：**改引擎时务必保持 `cross-speed.test.js` 绿，改装配时务必保持
`app-wiring.test.js` 里那条指纹对拍绿** —— 后者是唯一能自动发现「UI 改动破坏了
确定性」的手段。

---

## 九、推进路线建议

**第一天** —— P1-2 残留：给 `scheduler`（或 `GameFlow` 写入路径）补一道解锁校验，
防手改/旧版存档越级用技能；顺带决定「未解锁技能进了序列」时是拒绝施放还是自动剔除。

**第一周** —— P1-5 残留（`GAME_STATUS.PAUSED` 真正被写入 + P 键绑定）→
P1-1 残留（`logLimit` 只做展示裁剪、`showDamageNumbers` 去留）。每完成一项跑 `npm run verify`。

**第二周** —— **P1-6 胜利条件的产品决策**（用附录数据定终点层，同时决定
`SKILL_UNLOCK_MAX_LEVEL` 与经验曲线是否收紧），然后实现 `outcome: 'victory'`
清第八节列的新缺口（内容 apply 校验、Store 订阅重入、焦点断言、测试耗时）。

**再往后** —— `mods/dev` 示例模组、真实音频资源。

---

## 十、文档现状

三份可信：`《命运轮回：FATE LOOP》完整设计计划书.md`（主设计）、
`《命运轮回》开发计划书.md`（注意：这份其实描述的是旧项目形态，见下）、
`技术实施细化方案.md`（版本号已修正）。

⚠️ **`《命运轮回》开发计划书.md` 其实也是旧项目的**：标题即《命运轮回：Boss Rush》
开发计划书，通篇讲弹幕、擦弹、P 键暂停、灵魂碎片。上一版交接文档把它列为可信
文档是错的。已在文件名与内容层面保留原样（历史参考），但**不要按它实施**；
真正的设计以《完整设计计划书》为准。

`docs/archive/` 下两份（`阶段2进度.md`、`开发两轮审查流程.md`）已加「属于旧项目」声明抬头。

---

## 十一、一句话总结

内核扎实，阶段 9 的多屏 UI **已接好并有 61 例 jsdom 测试兜着**（冒烟 + 细测），
商店永久属性那个真 bug 已修且有三道防线，开局胜率坍塌也已修（38% → 76%）并有
回归门。**现在真正卡住的只剩一件事：没有胜利结局（P1-6）** —— 它是产品决策而不是
代码缺口，并且会顺带决定 `SKILL_UNLOCK_MAX_LEVEL=120` 这条永远吃不到的解锁尾巴。
接手第一件事是拿附录的数据把终点层定下来。

---

## 附录：本轮实测数据（供决策用，非承诺）

用 `src/core/progression.js` 现有函数测算，假设每层清 8 场战斗（6 普通 + 2 精英）、
每场平均 2.5 只怪：

| 层数 | 1 | 5 | 10 | 15 | 20 | 30 | 40 | 50 | 60 |
|---|---|---|---|---|---|---|---|---|---|
| 等级 | 6 | 18 | 26 | 31 | 35 | 41 | 45 | 48 | 51 |

- 满级 `MAX_LEVEL = 120` 需要总经验 **207,169,349**；60 层只能攒到约 305,550。
- 技能解锁把 84 个受限技能铺在 2~120 级：到 41 级（第 30 层）只能见到约 1/4 技能，
  oGCD 一个都见不到（最早 79 级）。
- 若终点定在第 50 层，可达等级约 48，`SKILL_UNLOCK_MAX_LEVEL` 若保持 120 则
  解锁表后半段永远是死内容。
- 胜率实测（每档 6 个种子 × 第 1~3 层全部战斗节点，阵亡即换种子）：
  - 旧默认序列（非法，含 oGCD）：117/130 ≈ **90%**
  - 旧 GCD 序列去掉 oGCD：12/30 = **40%**
  - 新默认序列（1 级合法，无 oGCD）：11/29 ≈ **38%** ← 上一轮修 P1-2 后短暂的状态
  - **当前默认（6 GCD + secondWind/suddenStrike 两个 oGCD，1 级合法）：19/25 ≈ 76%**
    ← 已固化为 `balance.test.js` 的 ≥70% 下限断言
- 若将来把终点定在 30 层（可达 41 级），`SKILL_UNLOCK_MAX_LEVEL` 取 45 左右
  才能让「一局集齐大部分技能」成立；保持 120 则后半段解锁永远是死内容。
