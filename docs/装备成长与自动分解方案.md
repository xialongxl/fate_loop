# 装备成长与自动分解方案

> 状态：**开工中** —— P0 + P3 已落地，P1/P2/P4 待做。每包一个 commit，进度见第八节。
> 起因是一串递进的问题：战斗日志太朴素 → 玩家名不能改 → 数值不膨胀 →
> 膨胀要能设计 → 设计成长曲线就得先让装备系统有后期。
> 日期：2026-08-31

---

## 一、本轮已定的两个决策

### 决策 1：品质从 6 档扩到 **9 档**（不止到传说）

当前只有 `破损/普通/精良/卓越/史诗/传说`，**传说就是顶**（`constants.js:269`）。
对齐参考项目扩到 9 档，补 `神话 / 圣物 / 终焉`。

> ⚠️ 澄清：先前说的"不止到终焉"是**笔误**，本意是"**不止到传说**"。
> 所以品质是**有限延伸到 9 档**，不是无上限程序生成 —— 不做"终焉·II/III"那种延伸段。

### 决策 2：终焉精炼要做，但**消耗碎片**，不引入新货币

参考项目用的是"终焉精华"（分解终焉装备掉落，`engine.js:738`）。本作**不抄这个货币**，
理由记在这里以免下次又被翻出来讨论：

> 终焉精炼是**唯一让毕业套装继续用到后期**的手段。

如果它靠一个新货币（精华），那这个货币只能靠"分解终焉装备"获得 —— 而玩家恰恰
**舍不得分解**毕业装，等于把后期成长锁死在一个自相矛盾的循环里。
用碎片则没有这个问题：碎片是全局通用货币，玩家能主动选择"投资源保住这件装备"。

**待定细节**（明天要算）：费用曲线、每个词条的精炼上限、是否随层数涨价。

---

## 二、待决问题（明天开工前必须先定）

### ~~待决 A~~ → 已撤销：我原先说错了（记录在此以免有人再被这条吓住）

我一开始写的是"自动分解会改变背包→改变属性→改变战斗，所以种子不再唯一输入，
**开工前必须先定架构**"。**这条判断是错的**，三处事实错误：

1. **自动分解早就在跑** —— `game.js:282` 背包满时溢出装备自动折成碎片，
   还会写日志「背包已满，N 件装备自动分解」。所以这不是"手动变自动"，
   只是把触发条件从"包满"换成"规则"。
2. **过滤器根本不碰 RNG** —— `rollBattleLoot` 用
   `encounterStream(seed, floor, `${nodeId}:loot`)` 派生独立子流，
   装备在过滤器看到它之前**已经 roll 完**了。过滤器既不消费随机数也不重排它。
3. **这游戏的确定性从来不是"只看种子"** —— 路线选择、商店买什么、序列怎么排、
   穿哪件拆哪件，全是玩家决策，而游戏没有录操作日志。
   "分享种子"的语义一直是「同种子 + 同玩法决策」。

**唯一剩下的真区别（很小，不值得当阻塞项）**：本作刻意保证"速度模式不影响结果"
（`cross-speed.test.js` 专门守这条），也就是说除了种子与玩家操作，
没有任何*设置*能改变一局结果 —— 过滤器会是第一个破例的。

所以做法二选一，都不影响架构：
- 省事：什么都不做。它和今天的手动分解同类，本来就属于"玩家决策"
- 严谨：存档里顺手记一个 `filterHash`（与 `contentHash` 同一个位置，几行），
  两人对比战绩对不上时有个解释

结论：**P2 可以直接开工，不需要先定架构。**

### 待决 B：9 档的数值怎么给 → 已定（2026-09-01）

`mult` 参考是 `0.5 1.0 1.5 2.2 3.5 6.0 10 15 22`（几何）。我们的 affixMax/orbSlots
现在是**每档手写**（`constants.js:264-269`），扩到 9 档要改成推导，否则三处会互相漏。

**决议**：
- `mult` 继续手写（它是设计意图本身，推导出来只是把常数换个地方藏）：
  前三档沿用现值，新档 **9.5 / 14.5 / 22**
- `affixMax` 改为**由下标推导**（`= index` → 0..8），`cls` 同理（`= q${index}`）
- `orbSlots` **删字段**。它全仓库无消费者（交接文档 §七 那条 P1 遗留项本来就写着
  「要么实装要么删」），而 P4 的精炼才是本作真正需要的「后期再加一层」

### 待决 C：精炼的具体曲线（见决策 2）→ 已定（2026-09-01）

**单件等级制，不按词条分别精炼**：上限 10 级，费用 `120 × 1.42^级`（满级累计≈ 7.6k 碎片），
每级主属性 +12% 且暴击词条 +0.6%，**不随层数涨价**（同层同装同价，跟本作
「不引入概率消耗」的既有纪律一致），分解时按已投入量部分返还。
理由见第八节 P4 条目。

---

## 三、参考项目读到的事实

路径 `F:/down/Idle_Game_ui_zero`（lit + 原生 JS 的放置挂机）。

```js
// js/data.js:68  9 档品质
{ 破损 0.5 } { 普通 1.0 } { 精良 1.5 } { 卓越 2.2 } { 史诗 3.5 }
{ 传说 6.0 } { 神话 10.0 } { 圣物 15.0 } { 终焉 22.0 }        // q0..q8
```

```js
// js/engine.js:64  LootFilter —— 不是"分解到某个品质"，是一套规则对象
{ minRarity: 5, requiredAffixes: ['crit'], minAffixValues: { crit: 5 } }
// 优先级：部位规则 > 全局规则 > 旧版品质阈值下拉框（兼容保留）
// 预设：finale_only(≥终焉) / mythic_above(≥神话) / high_crit_accessory(项链戒指遗物 ≥传说且 crit≥5)
// js/engine.js:105 shouldKeep()
```

- **自动熔炼发生在拾取瞬间**（`engine.js:869`）：不合格直接分解，并写一条日志
  `♻️ 自动熔炼了 [名]，获得 N G`（终焉装额外 `，获得 1 终焉精华`）
- 分解价值 `(rarityIdx + 1) × level × 8`（`engine.js:736`）
- 强化费用 `(rarityIdx + 1) × 300`，终焉装 11x —— 作者自己注明"是核心金币回收大户"
- 词条 3 种（冷却缩减 / 暴击率 / 共鸣）+ 宝珠 5 种，`MAX_SAME_ORB = 3`

---

## 四、我们项目的现状（已逐处核对）

| 项 | 现状 | 位置 |
|---|---|---|
| 品质 | 6 档，传说封顶，每档手写 `affixMax`/`orbSlots` | `src/core/constants.js:264-269` |
| 词缀 | 4 种 maxHp/attack/defense/crit | `src/core/constants.js:273-278` |
| 等级成长 | **纯线性** `+28hp +6atk +4def /级`，`MAX_LEVEL=120` | `constants.js:173-186`、`progression.js:baseStatsAtLevel` |
| 属性合成 | **四源加法** `base(level) + gear + seedBonus + permanentBonus` | `derived.js:derivePlayerStats` |
| 楼层缩放 | 线性 `hp×(1+0.12(f-1))`、`atk×(1+0.08(f-1))`，**无指数项** | `battle/encounter.js:57-58` |
| 强化 | `ENHANCE_MAX=10`，每级 `+8%` | `constants.js:281`、`equipment.js:enhanceGear` |
| 分解 | 手动 `salvage()` + 一键「分解破损与普通」+ **仅背包满时**自动分解溢出 | `game.js:284,304,729-740`、`equipmentScreen.js:48` |
| 分解价值 | `SALVAGE_BASE(4) × (rarity+1) × (1+enhance×0.2)` | `equipment.js:salvageValue` |
| 二级货币 | **无**。碎片是局内货币，跨局清零 | — |

### ⚠ 核对时发现的真 bug（阻塞一切膨胀方案）

```js
// src/core/equipment.js:53
function rollRarityIndex(rng, floorNumber) {
  const suppression = Math.min(0.9, Math.floor(floorNumber / 4) * 0.08);
  weight: index <= 1 ? r.weight * (1 - suppression) : r.weight   // ← 高品质权重是常数
}
```

层数**只让你更少捡到破烂，并不会让你更容易捡到传说**。打到 40 层之后玩家装备成长
基本停住 —— 这正是 demo 里"边际在第 40 层鼓包、之后一路走高"的**真实代码根源**。
这条不修，任何成长曲线方案都会在第 45 层断掉。

### 顺手要修的小错

`constants.js:277` 词缀 `crit` 的显示名写成 **「暴击」**（应为「暴击」），用户可见。

---

## 五、工作包与量

| 包 | 内容 | 主要文件 | 量 | 风险 |
|---|---|---|---|---|
| **P5** | 修「暴击」错字 | `constants.js` | ~2 行 | 无 |
| **P0** ✅ | 修 `rollRarityIndex`：高品质权重随层数提升 | `constants.js` `equipment.js` | +63 行（含 6 条回归） | 掉落期望变了 → 平衡测试会红。**实测不会**：前 3 层 progress≈0，balance 仍 97% |
| **P3** ✅ | 成长曲线可设计：新增 `GROWTH_BUDGET`（每层"玩家应多强/怪应多强"两张表），`baseStatsAtLevel`/楼层缩放/掉落权重改为读表 | `constants.js` `growth.js`(新) `progression.js` `encounter.js` `equipment.js` `tools/growth-budget-report.mjs`(新) | +420 行（含 23 条测试） | 改曲线=重跑平衡；**默认值逐项保持现状行为**（测试里拿旧闭式在 1..120 级 / 1..200 层逐位复算） |
| **P1** | 品质 6→9 档（含 affixMax/orbSlots 推导化、q6..q8 配色、图鉴与掉落文案） | `constants.js` `equipment.js` `styles.css` + 图鉴屏 | ~95 行 | 多处硬编码假设 6 档，要逐个查 |
| **P2** | 自动分解 LootFilter 化（规则模型 + 拾取即熔炼 + 编辑 UI + 3 预设 + 规则持久化） | 新增 `core/lootFilter.js`、`game.js`、新 UI 面板 | ~445 行 | 新 UI 要过 ui:audit。~~确定性风险~~ 已撤销：不碰 RNG，且包满自动分解早已存在 |
| **P4** | 终焉精炼（消耗碎片，词条再提升） | `equipment.js` `game.js` 装备屏 | ~250 行 | 依赖 P1/P3 定案；曲线要先算 |

**不含 P4 约 640 行，其中 UI 占 300。** 好消息：全部都不动存档结构、不动战斗引擎、
不动模组契约 —— 因为成长本来就是四源加法，12 个独立旋钮已经存在，
只是散在 5 个文件里没人总账。

### 会撞到的测试

`balance.test.js`（胜率下限守卫）、`equipment.test.js`/`loot.test.js`（品质档数、词缀名）、
内容数量守卫（新品质档会改变掉落）、`app-wiring.test.js`/`screens.test.js`（装备屏结构）、
`css-classes.test.js`（新类）。

### 建议顺序

**P5 → P0 → P3 → P1 → P2 → P4**
先把错字和"每层品质权重不涨"这条真 bug 修掉（它决定后面所有曲线有没有意义），
再让曲线可写，再动品质档数，最后做自动分解 UI 与精炼（最重、且依赖前面定案）。

---

## 六、本轮两个 demo 得到的结论（设计依据，别丢）

文件在 `demo/`（**尚未 commit**）：

- **`demo/battle-log.html`** —— 5 种日志写法（现状/战报/打击/极简/参考图语法）+ 3 种动效
  （飘字/卡片震动/受击闪）。定下的语法：`咏唱【技能】→ 造成 N 伤害（暴击！）附加[状态]`，
  技能名用全角【】、状态用半角[]、说明用全角（）；施法动词全游戏统一「咏唱」
  （避免给 90 个技能逐个填 cast 字段）；快照在 VM 里深冻，包写它当场报错而不是静默无效。
- **`demo/scale-curve.html`** —— 定点 BigInt 数值模型 + 5 旋钮反解 + 16 项分级诊断。

关键结论：

1. **判据必须是"安全边际"，不是"各自 TTK 区间"。**
   `边际 = 玩家扛得住的秒数 ÷ 打完全部怪要的秒数`（双方出手间隔不同，刀数不能直接比）。
   两条独立区间会放过"26 刀才打死一个怪 vs 只能扛 8 刀"这种**必输组合**。
2. **反解只约束控制点，中间段完全自由。** 实测三点全命中（2.71/1.96/2.50 vs 目标 2.7/2.0/2.5），
   但**第 11 层边际掉到 ×1.11**、第 40 层鼓包到 ×2.92。所以必须全程扫描，不能只看控制点。
3. **无尽段"时长恒定"与"边际恒定"由同一个比值控制**（`ttk ∝ (怪底/玩底)^250`，
   `margin ∝ (玩底/怪底)^500`）—— 一般不能同时满足，除非把玩家生命与攻击拆成两条曲线。
   （注：本作 `derivePlayerStats` 本来就是分属性加法，**这条游戏已经满足**，
   demo 里共用 `growthFP` 是我的简化。）
4. **膨胀可以完全做在显示层**：双方同曲线放大时，数字涨几十个数量级而 TTK 一秒不变。
5. **BigInt 不是换类型，是定点化**（`1.22^k`、`K/(K+defense)` 都是分数）。
   本轮决定**先不做** —— 把数值压在 `T`(1e12) 以内，离 `2^53≈9e15` 还有 3 个数量级余量。
6. 单位用 **K/M/B/T**（不用万亿兆）。

---

## 七、开工顺序（已按此执行）

1. ~~定 待决 A~~ 已撤销 → 2. 定 **待决 B/C**（已定，见第二节）→
3. **P5**（错字）→ **P0** → **P3** → **P1** → **P2** → **P4**
4. `demo/` 两个文件已随 P0 一并入库

---

## 八、落地进度

### P5 · 错字（已完成，用户直接改）

`constants.js` 词缀显示名与 `entity.js`/`damage.js` 注释里的「暂击」→「暴击」。

### P0 · 高品质权重随层数抬升（已完成）

- 新增 `GROWTH_BUDGET.loot`（别名 `LOOT_RARITY_CURVE`）：低档压制沿用旧值，
  高档按 `(1 + tierLift) ** (progress * (index - 1))` 抬升
- 抽了两个**纯函数**出来：`rarityWeightsAtFloor(floor)` / `expectedRarityAtFloor(floor)`。
  理由不是好看 —— 上一版曲线之所以能错那么久，就是因为没人能把权重摊开看
- 品质期望：E(1)=1.35 → E(25)=2.03 → E(45)=3.0 → E(90)=3.5，90 层后封顶（设计决定）
- 不消费随机数（`pickWeighted` 仍只调一次），所以不破坏三流分派
- **平衡实测**：`balance.test.js` 默认开局胜率仍 97%（163/168）—— 文档里那句
  「平衡测试会红」在 1~3 层这个射程内不成立，因为 progress 在那里接近 0

### P3 · GROWTH_BUDGET（已完成）

- `constants.js#GROWTH_BUDGET`：`player.perLevel`（分段每级成长）、
  `monster.{hp,attack,defense}`（分段倍率，`linear`/`compound`，**段界连续**：
  新段以前段外推值为起点，所以 50→51 不跳档）、`loot`、`targets`（设计对照组）
- 新 `src/core/growth.js` 只做一件事：**把表读成数**。`playerGrowthAtLevel` /
  `playerGrowthTotal`（闭式，不逐级 loop）/ `monsterScaleAtFloor` /
  `targetMarginAtFloor` / `validateGrowthBudget`（表自检）
- 三处改为读表：`progression.baseStatsAtLevel`、`encounter.instantiateMonsters`
  （原本在这里硬写 `0.12`/`0.08` 两个字面量）、`equipment` 的掉落曲线
- 新 `npm run growth:report`（`tools/growth-budget-report.mjs`）：**跑真引擎**全程扇描，
  判据用安全边际（结论 1），而不是各自 TTK 区间。它把第六节那些 demo 结论
  从"一次性调研"变成常驻手段。实测（seed 20240101，greedy 换装、固定开局序列）：
  第 5~6 层边际 ×17~68（偏松，因为玩家防御已到 320 而 K=100 是常数）、
  第 11 层起出现阵亡、第 26 层起崩（中位边际 0.54）。**两个结论**：
  ① 曲线不是"后期才坡"，是"中段就坡"；② K=100 与装备防御不缩放这个事实，
  比品质曲线更主导前期手感 —— 动曲线前先拿这个脚本扫一遍
- `--endless` 附送无尽段 compound 对照表（解析估算，**故意不重跑引擎**：
  `GROWTH_BUDGET` 是冻结常量，不应为了工具开一个能改变一局结果的后门；
  compound 机制本身由 `tests/unit/growth.test.js` 守）
- 新 `tests/unit/growth.test.js`（23 例）：默认表在 1..120 级与 1..200 层
  **逐位复算等于旧闭式**（这就是"P3 不改曲线"的硬证据）、分段数学、
  8 种坏表必须报错、以及"运行时真在读表"的接线断言

### 待做

P1（9 档）→ P2（自动分解）→ P4（终焉精炼）。
